const mongoose = require('mongoose');
exports.user = new mongoose.Schema({
    location: Uin
    firstname: String,
    lastname:String,   
    age: Number,
    passwordHash: String,
    phone: String,
    type: String,
});